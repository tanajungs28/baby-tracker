# 新生児育児記録アプリ「(仮称) BabyLog」要件定義書

Claude Code でこのファイルを読み込み、アプリ開発を進めてください。

---

## 1. プロジェクト概要

新生児の授乳・搾乳・ミルク・尿・便を1時間単位で記録し、グラフで可視化できる Web アプリ。寝不足の親がタッチ操作だけで素早く記録できることを最優先とする。

### 1.1 ゴール
- ワンタップ〜数タップで記録完了
- 24時間×項目のマトリクス表示で「いつ何をしたか」が一目で分かる
- 期間を切り替えてグラフで傾向を把握できる
- 新生児を育てる親が癒される、やさしく可愛い UI

---

## 2. 技術スタック

| 種別 | 採用技術 |
|------|---------|
| フレームワーク | Next.js 15（App Router）+ TypeScript |
| UI | React 19、Tailwind CSS、shadcn/ui |
| グラフ | Recharts |
| アイコン | lucide-react |
| 状態管理 | React Context + SWR（クライアントキャッシュ） |
| データベース | Supabase (PostgreSQL) |
| 認証 | Supabase Auth（メール/Google OAuth） |
| ホスティング | Vercel |
| パッケージマネージャ | pnpm |
| Lint/Format | ESLint + Prettier |

### 2.1 ディレクトリ構成（推奨）
```
src/
├── app/
│   ├── (auth)/login/page.tsx
│   ├── (app)/
│   │   ├── daily/page.tsx        # デイリー記録画面
│   │   ├── summary/page.tsx      # サマリ画面
│   │   └── layout.tsx            # 下部ナビ含む共通レイアウト
│   ├── api/                      # 必要なら BFF
│   └── layout.tsx
├── components/
│   ├── ui/                       # shadcn/ui
│   ├── daily/                    # マトリクス、入力モーダル
│   └── summary/                  # グラフ
├── lib/
│   ├── supabase/                 # client/server クライアント
│   ├── types.ts
│   └── utils.ts
└── hooks/
```

---

## 3. データモデル

### 3.1 records テーブル
| カラム | 型 | 説明 |
|--------|-----|------|
| id | uuid (PK) | レコードID |
| user_id | uuid (FK) | 認証ユーザーID |
| baby_id | uuid (FK) | 赤ちゃんID（将来複数対応のため） |
| recorded_date | date | 記録日（YYYY-MM-DD） |
| recorded_hour | int (0-23) | 時間帯 |
| type | enum | `breastfeeding` / `pumping` / `formula` / `pee` / `poop` |
| count | int | 授乳・尿・便の回数 |
| amount_ml | int | 搾乳・ミルクの量（5ml刻み、null可） |
| created_at | timestamptz | |
| updated_at | timestamptz | |

UNIQUE制約: `(user_id, baby_id, recorded_date, recorded_hour, type)`

### 3.2 babies テーブル
| カラム | 型 | 説明 |
|--------|-----|------|
| id | uuid (PK) | |
| user_id | uuid (FK) | |
| name | text | 赤ちゃんの名前 |
| birth_date | date | 生年月日 |
| created_at | timestamptz | |

### 3.3 Row Level Security
全テーブルで `user_id = auth.uid()` の RLS を有効化。

---

## 4. 機能要件

### 4.1 記録項目（5種類）

| 項目 | 入力方法 | 単位 |
|------|---------|------|
| 授乳 (breastfeeding) | タップで +1 | 回 |
| 搾乳 (pumping) | 5ml刻みで量を選択 | ml |
| ミルク (formula) | 5ml刻みで量を選択 | ml |
| 尿 (pee) | タップで +1 | 回 |
| 便 (poop) | タップで +1 | 回 |

- 量の選択範囲: 5〜200ml（5ml刻み）
- 1時間内に複数回ミルクを与えた場合は合算記録、または複数レコード保存可（実装はシンプル優先で「合算」推奨）

### 4.2 デイリー記録画面 (`/daily`)

#### レイアウト
- 上部: 日付ナビゲーション（◀ 2026/05/20 ▶ ／「今日」ボタン）
- 中央: **24行 × 5列のマトリクス**
  - 縦軸: 0:00 〜 23:00
  - 横軸: 授乳 / 搾乳 / ミルク / 尿 / 便
  - 各セル: 現在の値を表示（例: `+++`、`60ml`、`2回`）。空セルは薄いプレースホルダ。
- セルタップ →「入力モーダル」が下からスライドアップ

#### 入力モーダル
- 授乳・尿・便: 大きな「＋」「−」ボタン + 数値表示
- 搾乳・ミルク: 横スクロール式のホイール／ステッパー（5,10,15...ml）
- 「保存」「クリア」「キャンセル」ボタン
- キーボード入力は一切使わない

#### ハイライト
- 「現在時刻の行」を強調表示（薄い色背景＋アイコン）
- 当日の場合は自動でその行へスクロール

### 4.3 サマリ画面 (`/summary`)

#### 期間切り替えタブ
- 日 / 週 / 月（全部切り替え可能）

#### 表示内容
| グラフ | 内容 |
|--------|------|
| 授乳回数の推移 | 棒グラフ（日別/週別/月別合計） |
| ミルク・搾乳の量 | 積み上げ棒グラフ（ml） |
| 尿・便の回数 | 折れ線グラフ |
| 1日の総まとめカード | 当日（または期間平均）の合計値をカード表示 |

- 期間ナビゲーション（前へ / 次へ / 今日に戻る）
- 空データ時は可愛いイラストとメッセージ（例: 「まだ記録がないよ」）

### 4.4 共通ナビゲーション
- 下部固定のタブバー（モバイルファースト）
  - 「きろく」(daily) / 「グラフ」(summary) / 「せってい」(settings)
- 各タブは大きめのアイコン + 日本語ラベル

### 4.5 認証
- Supabase Auth でメール/Google
- 未ログインは `/login` へリダイレクト
- 初回ログイン時に赤ちゃんの情報（名前・生年月日）を登録

---

## 5. UI / UX デザイン要件

### 5.1 デザインコンセプト
**「ふんわり、やさしい、ねむそうでも押せる」**

### 5.2 カラーパレット
- ベース: クリーム白 `#FFF9F2`
- アクセント1: ベビーピンク `#FFD6E0`
- アクセント2: ベビーブルー `#CDE8FF`
- アクセント3: ミントグリーン `#D4F1D4`
- テキスト: チャコールブラウン `#5C4A3D`（純黒は使わない）
- 強調: コーラルピンク `#FF8FA3`

### 5.3 項目別カラー
- 授乳: ピンク 🌸
- 搾乳: ラベンダー 💜
- ミルク: クリームイエロー 🍼
- 尿: スカイブルー 💧
- 便: アースブラウン 🟤

### 5.4 タイポグラフィ
- 日本語: M PLUS Rounded 1c（丸ゴシック）
- 数値: Quicksand or Nunito

### 5.5 操作感
- タップ領域は最小 56×56px
- ボタン押下時の触覚的なアニメーション（軽くスケール）
- すべての操作でキーボードを呼び出さない
- 主要操作は片手・親指の届く範囲（下半分）に配置

### 5.6 アクセシビリティ
- コントラスト比 AA 以上
- aria-label を全インタラクティブ要素に
- prefers-reduced-motion 対応

---

## 6. 非機能要件

- レスポンシブ（モバイル優先、タブレットも見やすく）
- PWA 化（ホーム画面追加、オフライン記録 → 後で同期）
- 初回ロード 3秒以内
- 楽観的更新（記録は即時 UI 反映、裏で Supabase へ送信）
- エラー時はトーストで通知し、再試行可能

---

## 7. 開発ステップ（Claude Code への指示）

以下の順で実装してください。

1. **セットアップ**
   - `pnpm create next-app@latest` で TypeScript + Tailwind + App Router
   - shadcn/ui 初期化、必要コンポーネント追加（button, dialog, tabs, toast）
   - Supabase プロジェクト作成手順を README に記載
   - 環境変数: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`

2. **DB マイグレーション**
   - `supabase/migrations/` に SQL を配置
   - 上記スキーマと RLS ポリシーを作成

3. **認証フロー**
   - `/login` ページ、middleware で保護
   - 初回ログイン時の赤ちゃん登録モーダル

4. **デイリー記録画面**
   - マトリクス UI
   - 入力モーダル（5項目分のバリエーション）
   - 楽観的更新フック作成

5. **サマリ画面**
   - 日/週/月タブ
   - Recharts でグラフ実装
   - 期間ナビゲーション

6. **共通**
   - 下部タブバー
   - トースト通知
   - PWA マニフェスト

7. **仕上げ**
   - ローディング・空状態のイラスト
   - エラーバウンダリ
   - README に起動方法・デプロイ方法を記載

---

## 8. 受け入れ条件

- [ ] キーボードを一切使わずに5項目すべてを記録できる
- [ ] 1時間単位×24時間×5項目のマトリクスが1画面で確認できる
- [ ] サマリで日/週/月を切り替えてグラフが表示される
- [ ] 別端末でログインしても同じデータが見える
- [ ] スマホで片手操作でストレスなく使える
- [ ] 「可愛い」デザイン要件を満たしている

---

## 9. 将来拡張（今回は実装しない）

- 複数の赤ちゃん管理
- 体重・身長・体温の記録
- メモ・写真添付
- 家族との共有（招待リンク）
- 通知（次の授乳時間リマインダー）
- CSV エクスポート
